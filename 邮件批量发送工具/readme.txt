1、使用此工具，需启用excel宏功能

2、不要改动文件夹的结构，保留附件、邮件模板、工具在同一级目录下。

3、把需要发送的附件放在附件文件夹中，并在“工具表-附件配置页”中添加附件名称，注意，需保留文件后缀

4、把需要发送的邮件正文模板放在邮件模板文件夹中，并在“工具-基本配置”里面添加邮件模板名称，注意格式。

5、邮件模板中，可通过“<-|办公电话|->”的形式，标示自定义字段

5、在数据源页面中，选择发送邮箱，收件人等信息即可发送邮件

6、数据源中的新增变量区，可添加需要自定义在邮件正文中的字段信息，需与邮件模板中的配置一致